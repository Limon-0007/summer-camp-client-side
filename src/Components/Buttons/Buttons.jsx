import React from 'react';

const Buttons = ({title}) => {
    return (
        <button className="btn btn-outline border-0 border-b-4">
        {title}
      </button>
    );
};

export default Buttons;